#include <stdio.h>

/**
 * main - prints with proper grammar, but the outcome is a piece of art with new line
 * Return: (0) if program runs properly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
